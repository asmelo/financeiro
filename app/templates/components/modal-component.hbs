{{#ember-wormhole to="ember-wormhole-imagem" renderInPlace=renderInPlace }}
<div id="{{name}}" class="modal {{class}}" style={{style}}>
  <div class="modal-header">
    {{yield header}}
  </div>
  <div class="modal-content">
    {{yield content}}
  </div>
  <div class="modal-footer">
    {{yield footer}}
  </div>
</div>
{{/ember-wormhole}}
